{
    "version": 2,
    "builds": [
      { "src": "main.py", "use": "@vercel/python" }
    ],
    "buildCommand": "python -m uvicorn main:app --host 8080",
    "routes": [
      { "src": "/(.*)", "dest": "/main.py" }
    ],
    "env": {
      "APP_MODULE": "main:app"
    }
  }